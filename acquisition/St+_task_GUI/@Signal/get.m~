function val = get(sig,prop_name)
% GET Get Signal property from the specified object
% and return the value. Property names are:
% Sig_comp,Swp_on,Seq_on,Static_value,Sweep,Seq_values.
switch prop_name
case 'Name'
    val = sig.name;
case 'Channel'
    val = sig.chan;
case 'Reps_comp'
    val = sig.Reps_comp;
case 'Total_trials'
    val = sig.name;

otherwise
    error([prop_name,' is not a valid Reps_comp property'])
end