function sig = Signal(name,channel)
%   Signal class constructor.
%   Signal(name,channel) constructs a signal with a given name and with a
%   given channel.

if nargin==2
sig.name=name;
sig.chan=channel;
sig.Reps_comp=Reps_comp;
sig.total_trials=;
sig=class(sig,'Signal');
else
    error('Wrong number of input arguments for Signal constructor')
end